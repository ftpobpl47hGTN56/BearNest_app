<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/coordinator_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">
    <!-- ↑ fitsSystemWindows="true" — CoordinatorLayout сам добавляет отступ под
         статус-бар и навигационную панель. Всё содержимое начинается ниже статус-бара. -->

    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="?attr/actionBarSize"/>
    <!-- ↑ НЕТ app:layout_behavior — он перехватывал события NestedScrollView
         и "глотал" скролл, т.к. в CoordinatorLayout нет AppBarLayout.
         Убрали → NestedScrollView в HomeFragment и SettingsFragment скроллится нормально. -->

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        app:menu="@menu/bottom_nav_menu"
        app:backgroundTint="?attr/colorSurface"
        app:itemIconTint="@drawable/bottom_nav_item_color"
        app:itemTextColor="@drawable/bottom_nav_item_color"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
